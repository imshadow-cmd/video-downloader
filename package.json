{
  "name": "universal-video-downloader",
  "version": "1.0.0",
  "description": "Universal video downloader with zero-storage streaming via yt-dlp + FFmpeg",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
